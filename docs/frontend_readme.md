# 人群画像前端 (Portrait Web)

智能外呼客户画像系统前端项目，基于 Vue 3 + TypeScript + Vite 构建。

## 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 框架 | Vue 3 + TypeScript | 现代化前端框架 |
| 构建 | Vite | 快速开发服务器 |
| UI | Element Plus | 蓝色主题组件库 |
| 图表 | ECharts | 饼图 + 折线图 |
| HTTP | Axios | API 调用 |
| 部署 | Docker + Nginx | 容器化部署 |

## 功能特性

- 📊 **人群画像分布** - 环形饼图展示投诉风险、流失风险、满意用户占比
- 📈 **画像趋势变化** - 折线图展示历史趋势
- 📋 **用户明细列表** - 支持搜索、分页、导出
- 🔄 **多维度筛选** - 按场景、统计粒度(周/月/季)、时间周期筛选

## 快速开始

### 开发环境

```bash
# 1. 安装依赖
cd web
npm install

# 2. 启动后端 (在另一个终端)
cd ..
docker compose -f docker/docker-compose.yml up -d postgres
uv run uvicorn src.main:app --host 0.0.0.0 --port 8000

# 3. 启动前端开发服务器
cd web
npm run dev
```

前端访问地址: http://localhost:3001  
后端 API 地址: http://localhost:8000

### Docker 部署

```bash
cd web
docker-compose up -d
```

## 目录结构

```
web/
├── src/
│   ├── main.ts           # 入口文件
│   ├── App.vue           # 根组件
│   ├── views/            # 页面组件
│   │   └── PortraitDashboard.vue  # 主页面
│   ├── components/       # 通用组件
│   ├── api/              # API 调用模块
│   │   └── index.ts
│   ├── types/            # TypeScript 类型定义
│   │   └── index.ts
│   └── styles/           # 样式文件
│       └── main.css      # 蓝色主题样式
├── Dockerfile            # Docker 镜像配置
├── nginx.conf            # Nginx 配置
└── docker-compose.yml    # Docker Compose 配置
```

## API 接口

前端通过 Vite 代理调用后端 API:

| API | 方法 | 说明 |
|-----|------|------|
| `/api/v1/task` | GET | 获取任务/场景列表 |
| `/api/v1/task/{id}/summary` | GET | 获取任务汇总统计 |
| `/api/v1/task/{id}/trend` | GET | 获取趋势数据 |

## 开发说明

### 主题颜色

```css
--primary-color: #1677FF;      /* 主色蓝 */
--satisfied-color: #52C41A;    /* 满意用户 - 绿色 */
--complaint-color: #FF4D4F;    /* 投诉风险 - 红色 */
--churn-color: #FA8C16;        /* 流失风险 - 橙色 */
```

### 构建生产版本

```bash
npm run build
```

构建产物在 `dist/` 目录。

## 许可证

MIT
